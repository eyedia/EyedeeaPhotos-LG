# Local dev defaults (npm run dev) — activate page on the Cloud/web frontend
VITE_ACTIVATE_URL=http://192.168.86.100:5174/activate
# Device auth (issue/poll) uses the same host: http://192.168.86.100:5174/api/v1
# Override only if the API runs elsewhere:
# VITE_API_BASE_URL=http://localhost:3001/api/v1
